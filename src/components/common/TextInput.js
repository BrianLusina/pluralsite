import React from 'react';
import PropTypes from 'prop-types';

/**
 * TextInput stateless component
 */
const TextInput = ({myProps}) => {
    return (
        <div>
        </div>
    );
};

/**
 * Prop validation
 */
TextInput.propTypes = {
    myProps: PropTypes.string.isRequired
};

export default TextInput;
