import React from 'react';
import PropTypes from 'prop-types';

/**
 * SelectInput stateless component
 */
const SelectInput = ({myProps}) => {
    return (
        <div>
        </div>
    );
}

/**
 * Prop validation
 */
SelectInput.propTypes = {
    myProps: PropTypes.string.isRequired
}

export default SelectInput
